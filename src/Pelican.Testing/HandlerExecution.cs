namespace Pelican.Testing
{
    /// <summary>
    /// Assertion marker for the point where a request handler executes.
    /// </summary>
    public sealed class HandlerExecution
    {
        private HandlerExecution()
        {
        }
    }
}
